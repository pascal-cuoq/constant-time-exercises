/* Question 1: exponentiation. Compute a^n mod m where n is secret.
   Assume  a < m  and  m*m < UINT_MAX. 
   Reusing results from previous exercises is allowed. */

unsigned expn(unsigned a, unsigned n, unsigned m);

